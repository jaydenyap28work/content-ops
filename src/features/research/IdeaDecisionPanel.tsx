import{CheckCircle2,ExternalLink,PauseCircle,Pencil,X,XCircle}from'lucide-react'
import{Button,StatusBadge}from'../../components/ui'
import{useI18n}from'../i18n/i18n'
import{ideaFormatLabels,inferIdeaFormat}from'./idea-format'
import{planningStatusLabel}from'./idea-planner'
import type{IdeaRecord,ReferenceRecord}from'./research-api'

const sourceZh:Record<string,string>={douyin:'抖音',xhs:'小红书',tiktok:'TikTok',instagram:'Instagram',facebook:'Facebook',threads:'Threads',youtube:'YouTube',lemon8:'Lemon8',web:'网页'}
export function IdeaDecisionPanel({idea,references,busy,onClose,onEdit,onConfirm,onDecision}:{idea:IdeaRecord;references:ReferenceRecord[];busy:boolean;onClose:()=>void;onEdit:()=>void;onConfirm:()=>void;onDecision:(status:'paused'|'rejected')=>void}){const{language}=useI18n();const zh=language==='zh-CN';const format=idea.content_format??inferIdeaFormat(idea.suggested_format);const linked=idea.status==='converted'&&Boolean(idea.linked_content_id);const sources=references.filter(item=>idea.referenceIds.includes(item.id));const raw=idea.raw_content??idea.original_topic;return <div className="fixed inset-0 z-40 bg-ink/45 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className="ml-auto flex h-full w-full max-w-[86rem] flex-col bg-paper shadow-2xl lg:w-[80vw]"><header className="flex shrink-0 items-start justify-between gap-5 border-b border-line px-5 py-4 sm:px-8"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><StatusBadge>{planningStatusLabel(idea.planning_status,language)}</StatusBadge><span className="text-sm font-bold text-ink-muted">{ideaFormatLabels[format][zh?0:1]}</span>{idea.source_url?<a href={idea.source_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-sm font-bold text-blue">{zh?(sourceZh[idea.source_platform??'web']??'网页'):(idea.source_platform??'Web')}<ExternalLink className="size-3.5"/></a>:<span className="text-sm text-ink-muted">{zh?'原创':'Original'}</span>}</div><h2 className="mt-3 max-w-4xl font-display text-3xl font-semibold leading-tight sm:text-4xl">{idea.title}</h2></div><Button size="icon" variant="ghost" onClick={onClose} aria-label={zh?'关闭':'Close'}><X className="size-5"/></Button></header><div className="min-h-0 flex-1 overflow-y-auto"><div className="grid min-h-full lg:grid-cols-[minmax(0,1fr)_17rem]"><main className="space-y-8 px-5 py-6 sm:px-8 sm:py-8"><Section label={zh?'原始灵感':'Raw inspiration'} value={raw} prominent/><div className="grid gap-8 xl:grid-cols-2"><Section label={zh?'内容整理 / 概念':'Content concept'} value={idea.our_angle}/><Section label={zh?'为什么值得拍':'Why it is worth making'} value={idea.why_it_works}/><Section label={zh?'大概怎么玩':'How it could work'} value={[ideaFormatLabels[format][zh?0:1],idea.original_hook].filter(Boolean).join('\n\n')}/><Section label={zh?'负责人':'Owner'} value={idea.owner_name||idea.creator_name}/></div>{sources.length?<section><p className="text-xs font-bold text-ink-muted">{zh?'关联来源':'Linked sources'}</p><div className="mt-3 flex flex-wrap gap-2">{sources.map(source=><a key={source.id} href={source.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 border-b border-blue text-sm font-bold text-blue">{source.title}<ExternalLink className="size-3.5"/></a>)}</div></section>:null}{idea.notes?<Section label={zh?'内部备注':'Internal notes'} value={idea.notes}/>:null}</main><aside className="border-t border-line bg-canvas-raised/65 p-5 lg:border-l lg:border-t-0"><div className="sticky top-5 space-y-3"><p className="text-xs font-bold text-ink-muted">{zh?'老板决定':'Decision'}</p><Button className="w-full justify-center" disabled={busy} onClick={onConfirm}><CheckCircle2 className="size-4"/>{linked?(zh?'打开制作内容':'Open Production'):(zh?'确认拍摄':'Confirm to Shoot')}</Button>{!linked?<><Button className="w-full justify-center" variant="secondary" disabled={busy} onClick={()=>onDecision('paused')}><PauseCircle className="size-4"/>{zh?'暂缓':'Pause'}</Button><Button className="w-full justify-center" variant="danger" disabled={busy} onClick={()=>onDecision('rejected')}><XCircle className="size-4"/>{zh?'不采用':'Not Selected'}</Button><Button className="w-full justify-center" variant="ghost" disabled={busy} onClick={onEdit}><Pencil className="size-4"/>{zh?'编辑选题':'Edit Idea'}</Button></>:null}</div></aside></div></div></section></div>}
function Section({label,value,prominent=false}:{label:string;value:string|null|undefined;prominent?:boolean}){return <section><p className="text-xs font-bold text-ink-muted">{label}</p><div className={`mt-3 whitespace-pre-wrap text-ink-soft ${prominent?'border-l-4 border-coral bg-canvas-raised/55 px-5 py-4 text-[1.02rem] leading-8':'text-[.98rem] leading-7'}`}>{value||'—'}</div></section>}
