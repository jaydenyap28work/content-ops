import{useEffect,useState}from'react'
import type{FormEvent}from'react'
import{ChevronDown,ExternalLink,LoaderCircle,X}from'lucide-react'
import{Button,FormField,Input,Select,Textarea}from'../../components/ui'
import{useAuth}from'../auth/auth-context'
import{useI18n}from'../i18n/i18n'
import{ideaContentFormats,ideaFormatLabels,inferIdeaFormat}from'./idea-format'
import type{IdeaContentFormat}from'./idea-format'
import{loadContributorOptions,saveIdea}from'./research-api'
import type{ContributorOption,IdeaRecord,ResearchCatalog}from'./research-api'
import{parseSourceInput}from'./source-url'

interface FormState{clientId:string;title:string;sourceUrl:string;contentFormat:IdeaContentFormat;rawContent:string;ourAngle:string;whyItWorks:string;originalHook:string;ownerUserId:string;categoryId:string;tags:string;priority:string;plannedDate:string;notes:string}
const tags=(value:string)=>[...new Set(value.split(/[,，]/u).map(item=>item.trim()).filter(Boolean))]
export function QuickIdeaForm({workspaceId,idea,catalog,onClose,onSaved}:{workspaceId:string;idea:IdeaRecord|null;catalog:ResearchCatalog;onClose:()=>void;onSaved:(message:string)=>Promise<void>}){
 const{session}=useAuth();const{language}=useI18n();const zh=language==='zh-CN';const defaultClient=catalog.clients.find(item=>item.is_default_brand)?.id??catalog.clients[0]?.id??''
 const[form,setForm]=useState<FormState>(()=>({clientId:idea?.client_id??defaultClient,title:idea?.title??'',sourceUrl:idea?.source_url??'',contentFormat:idea?.content_format??inferIdeaFormat(idea?.suggested_format),rawContent:idea?.raw_content??idea?.original_topic??'',ourAngle:idea?.our_angle??'',whyItWorks:idea?.why_it_works??'',originalHook:idea?.original_hook??'',ownerUserId:idea?.owner_user_id??session?.user.id??'',categoryId:idea?.category_id??'',tags:idea?.tags.join(', ')??'',priority:idea?.priority??'normal',plannedDate:idea?.planned_date??'',notes:idea?.notes??''}))
 const[owners,setOwners]=useState<ContributorOption[]>([]);const[busy,setBusy]=useState(false);const[error,setError]=useState('')
 useEffect(()=>{if(!form.clientId)return;let active=true;void loadContributorOptions(form.clientId).then(data=>{if(active)setOwners(data)}).catch(e=>{if(active)setError(e instanceof Error?e.message:'Unable to load owners')});return()=>{active=false}},[form.clientId])
 const source=parseSourceInput(form.sourceUrl)
 async function submit(event:FormEvent){event.preventDefault();const parsed=parseSourceInput(form.sourceUrl);if(form.sourceUrl.trim()&&!parsed.url){setError(zh?'没有检测到有效链接，请检查后再试':'No valid link was detected');return}setBusy(true);setError('');try{await saveIdea(workspaceId,{id:idea?.id,clientId:form.clientId,title:form.title,plannedDate:form.plannedDate,sourceUrl:parsed.url??'',sourcePlatform:parsed.platform,rawContent:form.rawContent,contentFormat:form.contentFormat,originalTopic:idea?.original_topic??'',originalHook:form.originalHook,whyItWorks:form.whyItWorks,ourAngle:form.ourAngle,categoryId:form.categoryId||null,suggestedFormat:idea?.suggested_format??'',priority:form.priority,ownerUserId:form.ownerUserId||null,notes:form.notes,referenceIds:idea?.referenceIds??[],tags:tags(form.tags),contributors:idea?.contributors??[]});await onSaved(zh?(idea?'选题已更新':'选题已建立'):(idea?'Idea updated':'Idea created'))}catch(e){setError(e instanceof Error?e.message:(zh?'无法保存选题':'Unable to save Idea'))}finally{setBusy(false)}}
 return <div className="fixed inset-0 z-50 flex justify-end bg-ink/45 backdrop-blur-sm" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><section className="flex h-full w-full max-w-5xl flex-col bg-paper shadow-2xl"><header className="flex shrink-0 items-center justify-between border-b border-line px-5 py-4 sm:px-8"><div><p className="text-xs font-bold text-coral">{zh?'快速记录':'Quick intake'}</p><h2 className="mt-1 font-display text-3xl font-semibold">{zh?(idea?'编辑选题':'新增选题'):(idea?'Edit Idea':'New Idea')}</h2></div><Button size="icon" variant="ghost" onClick={onClose} aria-label={zh?'关闭':'Close'}><X className="size-5"/></Button></header><form className="min-h-0 flex-1 overflow-y-auto px-5 py-5 sm:px-8" onSubmit={submit}>{error?<div role="alert" className="mb-4 border-l-4 border-coral bg-coral/8 px-4 py-3 text-sm text-coral-dark">{error}</div>:null}<div className="mx-auto max-w-4xl space-y-5"><FormField label={zh?'标题':'Title'} required><Input autoFocus required className="h-12 text-base" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder={zh?'一句话写下这条内容':'Describe the content in one line'}/></FormField><FormField label={zh?'来源链接':'Source link'} hint={zh?'可直接粘贴整段分享文字':'Paste a URL or full share text'}><Textarea rows={2} value={form.sourceUrl} onChange={e=>setForm({...form,sourceUrl:e.target.value})} placeholder={zh?'抖音、小红书、TikTok、Instagram、Facebook、Threads、YouTube、Lemon8 或网页':'Douyin, Xiaohongshu, TikTok, Instagram, Facebook, Threads, YouTube, Lemon8 or web'}/>{source.url?<a href={source.url} target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-1 text-xs font-bold text-blue">{zh?'已检测到来源':'Source detected'}<ExternalLink className="size-3.5"/></a>:null}</FormField><FormField label={zh?'内容形式':'Content format'}><Select value={form.contentFormat} onChange={e=>setForm({...form,contentFormat:e.target.value as IdeaContentFormat})}>{ideaContentFormats.map(value=><option key={value} value={value}>{ideaFormatLabels[value][zh?0:1]}</option>)}</Select></FormField><FormField label={zh?'原始内容 / 灵感':'Raw content / inspiration'} hint={zh?'可直接贴入主题、角色、完整脚本、镜头、台词、字幕与备注':'Paste the topic, cast, full script, scenes, dialogue, subtitles and notes'}><Textarea className="min-h-80 text-[1rem] leading-7" value={form.rawContent} onChange={e=>setForm({...form,rawContent:e.target.value})}/></FormField><details className="group border-t border-line pt-3"><summary className="flex cursor-pointer list-none items-center justify-between py-3 font-bold"><span>{zh?'更多资料':'More details'}</span><ChevronDown className="size-5 text-ink-faint transition group-open:rotate-180"/></summary><div className="grid gap-5 pb-6 pt-2 sm:grid-cols-2">{catalog.clients.length>1?<FormField label={zh?'品牌 / 客户':'Brand / Client'} required><Select disabled={Boolean(idea)} value={form.clientId} onChange={e=>setForm({...form,clientId:e.target.value,ownerUserId:''})}>{catalog.clients.map(client=><option key={client.id} value={client.id}>{client.name}</option>)}</Select></FormField>:null}<FormField label={zh?'内容方向':'Content direction'} className="sm:col-span-2"><Textarea rows={4} value={form.ourAngle} onChange={e=>setForm({...form,ourAngle:e.target.value})}/></FormField><FormField label={zh?'为什么值得拍':'Why it is worth making'} className="sm:col-span-2"><Textarea rows={4} value={form.whyItWorks} onChange={e=>setForm({...form,whyItWorks:e.target.value})}/></FormField><FormField label={zh?'Hook / 建议开场':'Hook / suggested opening'} className="sm:col-span-2"><Textarea rows={3} value={form.originalHook} onChange={e=>setForm({...form,originalHook:e.target.value})}/></FormField><FormField label={zh?'负责人':'Owner'}><Select value={form.ownerUserId} onChange={e=>setForm({...form,ownerUserId:e.target.value})}><option value="">{zh?'自动使用创建者':'Use creator automatically'}</option>{owners.map(owner=><option key={owner.user_profile_id} value={owner.user_profile_id}>{owner.display_name}</option>)}</Select></FormField><FormField label={zh?'分类':'Category'}><Select value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})}><option value="">—</option>{catalog.categories.filter(item=>item.client_id===null||item.client_id===form.clientId).map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</Select></FormField><FormField label={zh?'标签':'Tags'}><Input value={form.tags} onChange={e=>setForm({...form,tags:e.target.value})} placeholder={zh?'用逗号分隔':'Comma separated'}/></FormField><FormField label={zh?'优先级':'Priority'}><Select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}><option value="low">{zh?'低':'Low'}</option><option value="normal">{zh?'普通':'Normal'}</option><option value="high">{zh?'高':'High'}</option><option value="urgent">{zh?'紧急':'Urgent'}</option></Select></FormField><FormField label={zh?'目标发布日期':'Target publish date'}><Input type="date" value={form.plannedDate} onChange={e=>setForm({...form,plannedDate:e.target.value})}/></FormField><FormField label={zh?'内部备注':'Internal notes'} className="sm:col-span-2"><Textarea rows={4} value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></FormField></div></details></div><footer className="sticky bottom-0 mt-4 flex justify-end gap-2 border-t border-line bg-paper/95 py-4 backdrop-blur"><Button type="button" variant="ghost" onClick={onClose}>{zh?'取消':'Cancel'}</Button><Button disabled={busy||!form.title.trim()}>{busy?<LoaderCircle className="size-4 animate-spin"/>:null}{zh?'保存选题':'Save Idea'}</Button></footer></form></section></div>
}
